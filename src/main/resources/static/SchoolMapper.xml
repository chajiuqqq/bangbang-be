<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chajiu.bangbangbe.Mapper.SchoolMapper">
    <resultMap id="SchoolMapperMap" type="com.chajiu.bangbangbe.entity.School">
        <id column="id" property="id"></id>
        <result column="name" property="name"></result>
    </resultMap>

    <select id="listSchools" resultMap="SchoolMapperMap">
        select * from school
    </select>
    <select id="selectById" resultMap="SchoolMapperMap" >
        select * from school where id=#{id}
    </select>
    <select id="selectByName" resultMap="SchoolMapperMap" >
        select * from school where name=#{name}
    </select>
    <insert id="insert" parameterType="school" useGeneratedKeys="true" keyProperty="id">
        insert into school(name) values (#{name})
    </insert>
    <update id="updateNameById">
        update school set name=#{name} where id=#{id}
    </update>
    <delete id="deleteById" parameterType="int">
        delete from school where id=#{id}
    </delete>
</mapper>
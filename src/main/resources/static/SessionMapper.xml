<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chajiu.bangbangbe.Mapper.SessionMapper">
    <insert id="add" parameterType="Session">
        insert into session(Id,open_id,session_key,skey,last_login_time) values(#{id},#{openId},#{sessionKey},#{skey},#{lastLoginTime})
    </insert>
</mapper>